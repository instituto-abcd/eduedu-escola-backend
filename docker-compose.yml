version: '3.4'

services:

  database:
    container_name: database
    image: postgres:latest
    hostname: localhost
    ports:
        - 5432:5432
    environment:
        - POSTGRES_USER=postgres
        - POSTGRES_PASSWORD=senhaS3creta
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
